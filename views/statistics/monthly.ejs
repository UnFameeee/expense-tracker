<div class="dashboard-summary">
    <div class="summary-card" style="background-color: #fff;">
        <div class="icon" style="background-color: #38b000;">
            <i class="fas fa-calendar-alt"></i>
        </div>
        <div class="info">
            <h3><%= new Intl.NumberFormat('vi-VN').format(total) %> VND</h3>
            <p>Chi tiêu tháng <%= month %>/<%= year %></p>
        </div>
    </div>
    <div class="summary-card" style="background-color: #fff;">
        <div class="icon" style="background-color: #4361ee;">
            <i class="fas fa-receipt"></i>
        </div>
        <div class="info">
            <h3><%= expenses.length %></h3>
            <p>Số khoản chi tiêu</p>
        </div>
    </div>
    <div class="summary-card" style="background-color: #fff;">
        <div class="icon" style="background-color: #e63946;">
            <i class="fas fa-chart-line"></i>
        </div>
        <div class="info">
            <h3><%= new Intl.NumberFormat('vi-VN').format(total / new Date(year, month, 0).getDate()) %> VND</h3>
            <p>Trung bình mỗi ngày</p>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <h2><i class="fas fa-chart-pie"></i> Thống kê chi tiêu theo tháng</h2>
        <div class="stats-nav">
            <a href="/statistics" class="btn"><i class="fas fa-chart-bar"></i> Tổng quan</a>
            <a href="/statistics/daily" class="btn"><i class="fas fa-calendar-day"></i> Ngày</a>
            <a href="/statistics/weekly" class="btn"><i class="fas fa-calendar-week"></i> Tuần</a>
            <a href="/statistics/monthly" class="btn btn-primary"><i class="fas fa-calendar-alt"></i> Tháng</a>
            <a href="/statistics/yearly" class="btn"><i class="fas fa-calendar"></i> Năm</a>
        </div>
    </div>

    <div class="month-selector">
        <form action="/statistics/monthly" method="GET">
            <div class="form-row">
                <div class="form-group">
                    <label for="month"><i class="fas fa-calendar-month"></i> Tháng:</label>
                    <select id="month" name="month" onchange="this.form.submit()">
                        <option value="1" <%= month === 1 ? 'selected' : '' %>>Tháng 1</option>
                        <option value="2" <%= month === 2 ? 'selected' : '' %>>Tháng 2</option>
                        <option value="3" <%= month === 3 ? 'selected' : '' %>>Tháng 3</option>
                        <option value="4" <%= month === 4 ? 'selected' : '' %>>Tháng 4</option>
                        <option value="5" <%= month === 5 ? 'selected' : '' %>>Tháng 5</option>
                        <option value="6" <%= month === 6 ? 'selected' : '' %>>Tháng 6</option>
                        <option value="7" <%= month === 7 ? 'selected' : '' %>>Tháng 7</option>
                        <option value="8" <%= month === 8 ? 'selected' : '' %>>Tháng 8</option>
                        <option value="9" <%= month === 9 ? 'selected' : '' %>>Tháng 9</option>
                        <option value="10" <%= month === 10 ? 'selected' : '' %>>Tháng 10</option>
                        <option value="11" <%= month === 11 ? 'selected' : '' %>>Tháng 11</option>
                        <option value="12" <%= month === 12 ? 'selected' : '' %>>Tháng 12</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="year"><i class="fas fa-calendar-year"></i> Năm:</label>
                    <select id="year" name="year" onchange="this.form.submit()">
                        <% for(let y = new Date().getFullYear(); y >= 2020; y--) { %>
                            <option value="<%= y %>" <%= year === y ? 'selected' : '' %>><%= y %></option>
                        <% } %>
                    </select>
                </div>
            </div>
        </form>
    </div>

    <% if (expenses.length > 0) { %>
        <div class="stats-overview">
            <div class="chart-container">
                <h3>Chi tiêu theo danh mục</h3>
                <canvas id="categoryChart"></canvas>
            </div>

            <div class="category-distribution">
                <h3>Phân bố chi tiêu theo danh mục</h3>
                <% if (categoryTotals && categoryTotals.length > 0) { %>
                    <div class="pie-chart-container">
                        <canvas id="categoryPieChart"></canvas>
                    </div>
                    <div class="category-list">
                        <% categoryTotals.forEach(category => { %>
                            <div class="category-item">
                                <div class="category-name">
                                    <span class="category-badge">
                                        <% if (category.category === 'Ăn uống') { %>
                                            🍕
                                        <% } else if (category.category === 'Di chuyển') { %>
                                            🚗
                                        <% } else if (category.category === 'Mua sắm') { %>
                                            🛒
                                        <% } else if (category.category === 'Hóa đơn') { %>
                                            📄
                                        <% } else if (category.category === 'Giải trí') { %>
                                            🎮
                                        <% } else { %>
                                            📌
                                        <% } %>
                                        <%= category.category %>
                                    </span>
                                </div>
                                <div class="category-amount"><%= new Intl.NumberFormat('vi-VN').format(category.total) %> VND</div>
                                <div class="category-percent"><%= ((category.total / total) * 100).toFixed(1) %>%</div>
                            </div>
                        <% }); %>
                    </div>
                <% } else { %>
                    <p class="no-data">Không có dữ liệu danh mục cho tháng này</p>
                <% } %>
            </div>
        </div>

        <div class="expenses-list">
            <h3>Chi tiêu trong tháng</h3>
            <table class="expense-table">
                <thead>
                    <tr>
                        <th>Ngày</th>
                        <th>Mô tả</th>
                        <th>Danh mục</th>
                        <th>Số tiền</th>
                    </tr>
                </thead>
                <tbody>
                    <% expenses.forEach(expense => { %>
                        <tr>
                            <td><%= new Date(expense.date).toLocaleDateString('vi-VN') %></td>
                            <td><%= expense.description %></td>
                            <td>
                                <span class="category-badge">
                                    <% if (expense.category === 'Ăn uống') { %>
                                        🍕
                                    <% } else if (expense.category === 'Di chuyển') { %>
                                        🚗
                                    <% } else if (expense.category === 'Mua sắm') { %>
                                        🛒
                                    <% } else if (expense.category === 'Hóa đơn') { %>
                                        📄
                                    <% } else if (expense.category === 'Giải trí') { %>
                                        🎮
                                    <% } else { %>
                                        📌
                                    <% } %>
                                    <%= expense.category %>
                                </span>
                            </td>
                            <td class="amount"><%= new Intl.NumberFormat('vi-VN').format(expense.amount) %> VND</td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    <% } else { %>
        <div class="empty-message">
            <i class="fas fa-calendar-alt"></i>
            <p>Không có chi tiêu nào trong tháng <%= month %>/<%= year %></p>
            <a href="/expenses/new" class="btn btn-primary"><i class="fas fa-plus-circle"></i> Thêm chi tiêu mới</a>
        </div>
    <% } %>
</div>

<% if (expenses.length > 0) { %>
<script>
    // Biểu đồ chi tiêu theo danh mục
    const categoryCtx = document.getElementById('categoryChart').getContext('2d');
    const categoryData = <%= chartData %>;
    
    new Chart(categoryCtx, {
        type: 'bar',
        data: categoryData,
        options: {
            scales: {
                y: {
                    beginAtZero: true
                }
            },
            responsive: true,
            maintainAspectRatio: false
        }
    });
    
    <% if (categoryTotals && categoryTotals.length > 0) { %>
    // Biểu đồ phân bố chi tiêu theo danh mục
    const categoryPieCtx = document.getElementById('categoryPieChart').getContext('2d');
    const categoryPieData = {
        labels: [<% categoryTotals.forEach(category => { %>'<%= category.category %>', <% }); %>],
        datasets: [{
            data: [<% categoryTotals.forEach(category => { %><%= category.total %>, <% }); %>],
            backgroundColor: [
                'rgba(255, 99, 132, 0.7)',
                'rgba(54, 162, 235, 0.7)',
                'rgba(255, 206, 86, 0.7)',
                'rgba(75, 192, 192, 0.7)',
                'rgba(153, 102, 255, 0.7)',
                'rgba(255, 159, 64, 0.7)',
                'rgba(201, 203, 207, 0.7)'
            ],
            borderWidth: 1
        }]
    };
    
    new Chart(categoryPieCtx, {
        type: 'doughnut',
        data: categoryPieData,
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
                legend: {
                    position: 'right',
                }
            }
        }
    });
    <% } %>
</script>
<% } %>

<style>
    .month-selector {
        margin: 1.5rem 0;
        padding: 1rem;
        background-color: #f8fafc;
        border-radius: var(--border-radius);
    }
    
    .form-row {
        display: flex;
        gap: 1rem;
    }
    
    .form-row .form-group {
        flex: 1;
        display: flex;
        align-items: center;
        gap: 1rem;
    }
    
    .month-selector label {
        font-weight: 500;
        color: var(--dark-color);
        margin-bottom: 0;
        white-space: nowrap;
    }
    
    .month-selector select {
        padding: 0.5rem 1rem;
        border: 1px solid #e2e8f0;
        border-radius: var(--border-radius);
        background-color: white;
        flex: 1;
    }
    
    .stats-nav {
        display: flex;
        gap: 0.5rem;
    }
    
    .stats-overview {
        display: flex;
        flex-wrap: wrap;
        gap: 2rem;
        margin-top: 2rem;
    }
    
    .chart-container {
        flex: 1;
        min-width: 300px;
        height: 300px;
    }
    
    .category-distribution {
        flex: 1;
        min-width: 300px;
    }
    
    .pie-chart-container {
        height: 250px;
        margin-bottom: 1.5rem;
    }
    
    .category-list {
        max-height: 300px;
        overflow-y: auto;
    }
    
    .category-item {
        display: flex;
        justify-content: space-between;
        padding: 0.75rem 0;
        border-bottom: 1px solid #e2e8f0;
    }
    
    .category-name {
        flex: 2;
    }
    
    .category-amount {
        flex: 1;
        text-align: right;
    }
    
    .category-percent {
        flex: 0 0 60px;
        text-align: right;
        font-weight: 500;
    }
    
    .expenses-list {
        margin-top: 2rem;
    }
    
    h3 {
        margin-bottom: 1.5rem;
        color: var(--dark-color);
        font-size: 1.2rem;
    }
    
    .no-data {
        color: var(--text-muted);
        text-align: center;
        padding: 2rem 0;
    }
    
    @media (max-width: 768px) {
        .stats-nav {
            flex-wrap: wrap;
        }
        
        .stats-overview {
            flex-direction: column;
        }
        
        .form-row {
            flex-direction: column;
        }
    }
</style>
